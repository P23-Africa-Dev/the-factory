import { ProfilePage } from "@/components/profile/profile-page";

export const metadata = {
  title: "Profile — The Factory",
};

export default function DashboardProfilePage() {
  return <ProfilePage />;
}
