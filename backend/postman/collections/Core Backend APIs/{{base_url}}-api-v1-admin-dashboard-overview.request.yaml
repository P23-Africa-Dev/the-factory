$kind: http-request
name: "{{base_url}}/api/v1/admin/dashboard/overview"
url: "{{base_url}}/api/v1/admin/dashboard/overview?company_id={{company_id}}"
method: GET
headers:
  Accept: application/json
  Authorization: "Bearer {{access_token}}"
settings:
  disabledSystemHeaders: []
